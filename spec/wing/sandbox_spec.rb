require 'spec_helper'

describe Wing::Sandbox do
  it 'has a version number' do
    expect(Wing::Sandbox::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
